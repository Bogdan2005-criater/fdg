Вот обновленная версия с более подробными объяснениями для каждого задания:

---
https://doka.guide/js/array-splice/ - splice


### Инструкция по основам JavaScript для новичков

---

#### 1. **Переменные и типы данных**
**Объяснение**:  
Переменные хранят данные. В JS используются ключевые слова `let`, `const` (для констант) и устаревшее `var`.  
Основные типы данных:  
- Числа: `42`, `3.14`  
- Строки: `"Hello"`, `'JS'`  
- Булевы: `true`, `false`  
- `null` (отсутствие значения) и `undefined` (неопределенность).  
- Объекты и массивы (рассмотрим позже).

**Пример**:  
```javascript
let name = "Анна";
const age = 25;
let isStudent = true;
```

**Задачи**:  
1. Создайте переменную `city` и присвойте ей название вашего города.  
2. Объявите константу `PI` со значением 3.14.  
3. Создайте переменную `message`, которая объединяет строку "Меня зовут " и значение переменной `name`.  
4. Преобразуйте строку "123" в число и сохраните в переменную `number`.  
5. Проверьте тип данных переменной `isStudent` с помощью `typeof`.

---

#### 2. **Операторы и условия**
**Объяснение**:  
- Арифметические операторы: `+`, `-`, `*`, `/`, `%`.  
- Операторы сравнения: `===`, `!==`, `>`, `<`.  
- Логические операторы: `&&`, `||`, `!`.  
- Условия: `if`, `else if`, `else`, тернарный оператор `? :`.

**Пример**:  
```javascript
let x = 10;
if (x > 5) {
  console.log("Больше 5");
} else {
  console.log("Меньше или равно 5");
}
```

**Задачи**:  
1. Напишите условие, проверяющее, четное ли число.  
2. Создайте калькулятор, который складывает два числа, если они оба положительные.  
3. Используя тернарный оператор, определите, может ли пользователь войти (возраст ≥ 18).  
4. Проверьте, делится ли число на 3 и 5 одновременно.  
5. Напишите код, который выводит "утро", если время < 12, "день" < 18, иначе "вечер".

---

#### 3. **Циклы**
**Объяснение**:  
- `for`: для повторения блока кода заданное число раз.  
- `while`: пока условие истинно.  
- `do...while`: выполнить хотя бы один раз.  
- `break` (прервать цикл), `continue` (пропустить итерацию).

**Пример**:  
```javascript
for (let i = 0; i < 5; i++) {
  console.log(i); // 0, 1, 2, 3, 4
}
```

**Задачи**:  
1. Выведите числа от 1 до 10 с помощью `for`.  
2. Посчитайте сумму чисел от 1 до 100.  
3. Найдите индекс первого отрицательного числа в массиве `[5, 3, -2, 8]`.  
4. Выведите таблицу умножения на 7 (7×1=7, 7×2=14...).  
5. Сделайте обратный отсчет от 10 до 0 с помощью `while`.

---

#### 4. **Функции**
**Объяснение**:  
Функции объявляются через `function` или стрелочные `() => {}`.  
Они принимают параметры и возвращают результат через `return`.

**Пример**:  
```javascript
function add(a, b) {
  return a + b;
}
```

**Задачи**:  
1. Напишите функцию, которая возвращает квадрат числа.  
2. Создайте функцию, проверяющую, является ли число четным.  
3. Напишите функцию для конвертации градусов Цельсия в Фаренгейты.  
4. Создайте функцию, которая считает факториал числа.  
5. Напишите функцию, возвращающую наибольшее из двух чисел.

---

#### 5. **Массивы и методы**
**Объяснение**:  
Массив — упорядоченный список элементов: `let arr = [1, "текст", true]`.  
Основные методы:  
- `push()` / `pop()` (добавить/удалить в конец).  
- `shift()` / `unshift()` (удалить/добавить в начало).  
- `forEach()`, `map()`, `filter()`.

**Пример**:  
```javascript
let fruits = ["яблоко", "груша"];
fruits.push("апельсин"); // ["яблоко", "груша", "апельсин"]
```

**Задачи**:  
1. Создайте массив чисел и найдите их сумму.  
2. Отфильтруйте массив, оставив только числа больше 10.  
3. Преобразуйте массив строк в верхний регистр.  
4. Объедините два массива в один.  
5. Найдите дубликаты в массиве `[1, 2, 2, 3, 4, 4]`.

---

#### 6. **Объекты**
**Объяснение**:  
Объекты хранят данные в формате ключ-значение:  
```javascript
let user = {
  name: "Анна",
  age: 25,
  greet: function() {
    console.log("Привет!");
  }
};
```

**Задачи**:  
1. Создайте объект `книга` с полями `title`, `author`, `year`.  
2. Добавьте метод `getAge`, который возвращает возраст книги (текущий год – `year`).  
3. Посчитайте количество свойств в объекте.  
4. Объедините два объекта в один (например, `user` и `profile`).  
5. Проверьте, есть ли в объекте свойство `email`.

---

#### 7. **Обработка ошибок**
**Объяснение**:  
Используйте `try...catch` для перехвата ошибок:  
```javascript
try {
  // Код, который может вызвать ошибку
} catch (error) {
  console.log(error.message);
}
```

**Задачи**:  
1. Напишите код, который ловит ошибку деления на ноль.  
2. Обработайте ошибку при попытке преобразовать строку в число.  
3. Создайте функцию, которая выбрасывает ошибку, если аргумент не число.  
4. Реализуйте проверку ввода пароля (минимум 6 символов).  
5. Используйте `finally` для вывода сообщения после `try/catch`.

---

Каждая задача помогает закрепить теорию на практике. Постепенно усложняйте задания, добавляя комбинации изученных тем. Удачи в изучении JS! 🚀

### 1. Задача на условие if:
**Проверка положительности числа.**

**Решение:**

```javascript
let num = 5;
if (num > 0) {
  console.log("Число положительное");
} else {
  console.log("Число не положительное");
}
```

**Подробное объяснение:**  
1. Создаётся переменная `num` со значением 5.  
2. Используется конструкция `if`, которая проверяет условие `num > 0`.  
   - Если число больше нуля, выполняется блок `if`, и выводится сообщение `"Число положительное"`.  
   - Если условие ложно (число равно нулю или меньше нуля), выполняется блок `else`, выводящий `"Число не положительное"`.  
3. Таким образом, программа разделяет числа на положительные и остальные.

---

### 2. Задача на if с дополнительным условием:
**Проверка, является ли число положительным, отрицательным или нулём.**

---

### 3. Задача на функцию:
**Создание функции для нахождения суммы двух чисел.**

**Решение:**

```javascript
function sum(a, b) {
  return a + b;
}
console.log(sum(3, 4)); // 7
```

**Подробное объяснение:**  
1. Функция `sum` принимает два параметра `a` и `b`.  
2. Внутри функции используется оператор `+`, чтобы сложить значения `a` и `b`.  
3. Результат возвращается с помощью оператора `return`.  
4. При вызове функции `sum(3, 4)` в консоль выводится значение 7.

---

### 4. Задача на функцию с дополнительной операцией:
**Функция возвращает сумму и произведение двух чисел в виде объекта.**

---

### 5. Задача на цикл for:
**Вывод чисел от 1 до 5 с использованием цикла.**

**Решение:**

```javascript
for (let i = 1; i <= 5; i++) {
  console.log(i);
}
```

**Подробное объяснение:**  
1. Переменная `i` инициализируется значением 1.  
2. Цикл выполняется, пока `i <= 5`.  
3. На каждом шаге `i` увеличивается на 1 (операция `i++`).  
4. На каждом шаге в консоль выводится текущее значение `i`.  
5. Когда `i` становится больше 5, цикл завершается.

---

### 6. Задача на цикл for с условием:
**Вывод всех чётных чисел от 1 до 10.**

---

### 7. Задача на использование переменных:
**Создание строки с именем и возрастом.**

**Решение:**

```javascript
let name = "Иван";
let age = 25;
console.log(`Привет, ${name}! Тебе ${age} лет.`);
```

**Подробное объяснение:**  
1. Переменная `name` содержит строку `"Иван"`.  
2. Переменная `age` содержит число `25`.  
3. С помощью шаблонной строки (с использованием обратных кавычек ``) создаётся сообщение, включающее значения переменных.  

---

### 8. Задача на использование переменных с условием:
**Проверка возраста для вывода сообщения.**


---

### 9. Задача на функцию с параметрами:
**Нахождение среднего значения трёх чисел.**

**Решение:**

```javascript
function average(a, b, c) {
  return (a + b + c) / 3;
}
console.log(average(3, 4, 5)); // 4
```

**Подробное объяснение:**  
1. Функция `average` принимает три параметра `a`, `b` и `c`.  
2. Она возвращает результат деления суммы чисел `(a + b + c)` на 3.  
3. При вызове функции с аргументами 3, 4 и 5 возвращается их среднее значение — 4.

---

### 10. Задача на функцию с дополнительным условием:
**Проверка наличия отрицательных чисел перед вычислением среднего.**

---

### 11. Задача на цикл с изменением:
**Вывод чисел от 10 до 1 в обратном порядке.**

**Решение:**

```javascript
for (let i = 10; i >= 1; i--) {
  console.log(i);
}
```

**Подробное объяснение:**  
1. Переменная `i` инициализируется значением 10.  
2. Цикл выполняется, пока `i >= 1`.  
3. На каждом шаге значение `i` уменьшается на 1 (операция `i--`).  

---

### 12. Задача на цикл с изменением и условием:
**Вывод чисел от 10 до 1, пропуская чётные.**
